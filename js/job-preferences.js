import {esc} from './data.js?v=perf-1';
export function billingBadge(state,a){const job=state.jobs.find(j=>j.id===a.job),billed=(state.extras?.billing||[]).some(b=>b.job_id===a.job&&b.date===a.date&&b.locked);return job?.status==='invoiced'||billed?`<span class="value-pill billing-badge">✓ ${job?.status==='invoiced'?'Celá zakázka vyfakturována':'Den vyfakturován'}</span>`:''}
export function installJobPreferences(state,user){
 const grid=document.querySelector('#content .list-grid');if(!grid)return;const key='planner-job-preferences:'+user+':'+state.company.id;let prefs={filter:'all',sort:'name',group:'',groups:{},favorites:[]};try{Object.assign(prefs,JSON.parse(localStorage.getItem(key)||'{}'))}catch{}
 const cards=[...grid.children].map(card=>({card,job:state.jobs.find(j=>j.id===card.querySelector('[data-action=job]')?.dataset.id)})).filter(r=>r.job);
 const save=()=>{try{localStorage.setItem(key,JSON.stringify(prefs))}catch{}};
 const controls=document.createElement('div');controls.className='job-preferences';grid.before(controls);
 function renderControls(){controls.innerHTML=`<label>Zobrazení<select data-pref="filter">${[['all','Vše'],['active','Aktivní'],['invoiced','Vyfakturované'],['completed','Dokončené'],['unplanned','Bez budoucího plánu'],['favorites','Oblíbené']].map(([id,t])=>`<option value="${id}" ${prefs.filter===id?'selected':''}>${t}</option>`).join('')}</select></label><label>Řazení<select data-pref="sort">${[['name','Název A–Z'],['reverse','Název Z–A'],['favorites','Oblíbené nejdříve'],['status','Podle stavu']].map(([id,t])=>`<option value="${id}" ${prefs.sort===id?'selected':''}>${t}</option>`).join('')}</select></label><label>Moje skupina<select data-pref="group"><option value="">Všechny skupiny</option>${[...new Set(Object.values(prefs.groups).filter(Boolean))].sort().map(g=>`<option ${prefs.group===g?'selected':''}>${esc(g)}</option>`).join('')}</select></label><small>Vlastní skupiny a pořadí se pamatují pro tento účet v prohlížeči.</small>`;controls.onchange=e=>{if(e.target.dataset.pref){prefs[e.target.dataset.pref]=e.target.value;save();draw()}}}
 function draw(){const today=new Date().toLocaleDateString('sv-SE');cards.sort((a,b)=>prefs.sort==='favorites'?Number(prefs.favorites.includes(b.job.id))-Number(prefs.favorites.includes(a.job.id))||a.job.name.localeCompare(b.job.name,'cs'):prefs.sort==='status'?a.job.status.localeCompare(b.job.status)||a.job.name.localeCompare(b.job.name,'cs'):a.job.name.localeCompare(b.job.name,'cs')*(prefs.sort==='reverse'?-1:1)).forEach(({card,job:j})=>{const f=prefs.filter;card.hidden=!(f==='all'||f==='active'&&!['invoiced','completed','merged'].includes(j.status)||f===j.status||f==='unplanned'&&!state.assignments.some(a=>a.job===j.id&&a.date>=today)||f==='favorites'&&prefs.favorites.includes(j.id))||!!prefs.group&&prefs.groups[j.id]!==prefs.group;grid.append(card)})}
 cards.forEach(({card,job:j})=>{const row=document.createElement('div');row.className='job-personal';row.innerHTML=`<button type="button" aria-label="Oblíbená zakázka" aria-pressed="${prefs.favorites.includes(j.id)}">${prefs.favorites.includes(j.id)?'★':'☆'}</button><label>Moje skupina<input maxlength="60" value="${esc(prefs.groups[j.id]||'')}" placeholder="např. Servis, Priorita"></label>`;row.querySelector('button').onclick=e=>{prefs.favorites=prefs.favorites.includes(j.id)?prefs.favorites.filter(id=>id!==j.id):[...prefs.favorites,j.id];e.currentTarget.textContent=prefs.favorites.includes(j.id)?'★':'☆';e.currentTarget.setAttribute('aria-pressed',prefs.favorites.includes(j.id));save();draw()};row.querySelector('input').onchange=e=>{prefs.groups[j.id]=e.target.value.trim();save();renderControls();draw()};card.append(row)});renderControls();draw();
}
